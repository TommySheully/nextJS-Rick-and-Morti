import React from 'react';

type PaginationComponentType = {
    currentPage: number
    totalCount: number
}

const Pagination = ({totalCount, currentPage}: PaginationComponentType) => {

    return (<></>)

};

export default Pagination;